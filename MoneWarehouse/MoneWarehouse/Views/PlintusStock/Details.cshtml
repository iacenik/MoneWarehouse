@model EntityLayer.Entities.PlıntusStock

@{
    ViewData["Title"] = "Plıntus Stok Detayları";
}

<div class="container-fluid">
    @if (TempData["SuccessMessage"] != null)
    {
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            <i class="fas fa-check-circle me-1"></i> @TempData["SuccessMessage"]
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    }

    @if (TempData["ErrorMessage"] != null)
    {
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
            <i class="fas fa-exclamation-circle me-1"></i> @TempData["ErrorMessage"]
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    }

    <div class="row">
        <div class="col-md-5">
            <div class="card shadow mb-4">
                <div class="card-header py-3 d-flex justify-content-between align-items-center">
                    <h4 class="m-0 font-weight-bold text-primary">Stok Bilgileri</h4>
                    <div>
                        <a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-warning btn-sm rounded-pill px-3">
                            <i class="fas fa-edit me-1"></i> Düzenle
                        </a>
                        <a asp-action="Index" class="btn btn-secondary btn-sm rounded-pill px-3">
                            <i class="fas fa-arrow-left me-1"></i> Listeye Dön
                        </a>
                    </div>
                </div>
                <div class="card-body">
                    <div class="p-4">
                        <div class="mb-4 text-center">
                            <div class="display-4 font-weight-bold text-primary">@Model.Quantity</div>
                            <p class="text-muted">Mevcut Stok Miktarı</p>
                        </div>

                        <div class="row mb-3">
                            <div class="col-md-4 text-muted">Stok ID:</div>
                            <div class="col-md-8 font-weight-bold">@Model.Id</div>
                        </div>

                        <div class="row mb-3">
                            <div class="col-md-4 text-muted">Kod:</div>
                            <div class="col-md-8 font-weight-bold">@Model.Code</div>
                        </div>

                        <div class="row mb-3">
                            <div class="col-md-4 text-muted">İsim:</div>
                            <div class="col-md-8 font-weight-bold">@Model.Name</div>
                        </div>

                        <div class="row mb-3">
                            <div class="col-md-4 text-muted">Durum:</div>
                            <div class="col-md-8">
                                @if (Model.Quantity <= 5)
                                {
                                    <span class="badge bg-danger">Kritik Seviye</span>
                                }
                                else if (Model.Quantity <= 20)
                                {
                                    <span class="badge bg-warning text-dark">Düşük Stok</span>
                                }
                                else
                                {
                                    <span class="badge bg-success">Yeterli</span>
                                }
                            </div>
                        </div>

                        <hr class="my-4" />

                        <div class="d-grid gap-2">
                            <a asp-action="ManageStock" asp-route-id="@Model.Id" class="btn btn-outline-primary">
                                <i class="fas fa-balance-scale me-1"></i> Stok Miktarını Ayarla
                            </a>
                            <a asp-action="Delete" asp-route-id="@Model.Id" class="btn btn-outline-danger">
                                <i class="fas fa-trash-alt me-1"></i> Stok Kaydını Sil
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <div class="col-md-7">
            <div class="card shadow mb-4">
                <div class="card-header py-3">
                    <h4 class="m-0 font-weight-bold text-primary">Günlük Üretim Kayıtları</h4>
                </div>
                <div class="card-body">
                    @if (Model.PlıntusDailies != null && Model.PlıntusDailies.Any())
                    {
                        <div class="table-responsive">
                            <table id="dailyEntriesTable" class="table table-hover table-bordered">
                                <thead class="table-light">
                                    <tr>
                                        <th>ID</th>
                                        <th>Tarih</th>
                                        <th>Çalışan</th>
                                        <th>Miktar</th>
                                        <th>İşlemler</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var entry in Model.PlıntusDailies.OrderByDescending(e => e.Date))
                                    {
                                        <tr>
                                            <td>@entry.Id</td>
                                            <td>@entry.Date.ToString("dd.MM.yyyy")</td>
                                            <td>
                                                @if (entry.Employee != null)
                                                {
                                                    <span>@entry.Employee.EmployeeName</span>
                                                }
                                                else
                                                {
                                                    <span>Çalışan ID: @entry.EmployeeId</span>
                                                }
                                            </td>
                                            <td class="text-end">@entry.Quantity</td>
                                            <td>
                                                <a asp-controller="PlıntusDaily" asp-action="Details" asp-route-id="@entry.Id" class="btn btn-sm btn-outline-info" data-toggle="tooltip" title="Detaylar">
                                                    <i class="fas fa-info-circle"></i>
                                                </a>
                                            </td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        </div>

                        <div class="card mt-4">
                            <div class="card-header bg-info text-white">
                                <h5 class="m-0"><i class="fas fa-chart-pie me-1"></i> Üretim Özeti</h5>
                            </div>
                            <div class="card-body">
                                <div class="row">
                                    <div class="col-md-4 text-center mb-3">
                                        <div class="display-4 font-weight-bold">@Model.PlıntusDailies.Count()</div>
                                        <p>Toplam Kayıt Sayısı</p>
                                    </div>
                                    <div class="col-md-4 text-center mb-3">
                                        <div class="display-4 font-weight-bold">@Model.PlıntusDailies.Sum(x => x.Quantity)</div>
                                        <p>Toplam Üretim Miktarı</p>
                                    </div>
                                    <div class="col-md-4 text-center mb-3">
                                        <div class="display-4 font-weight-bold">@Model.PlıntusDailies.Select(x => x.EmployeeId).Distinct().Count()</div>
                                        <p>Toplam Çalışan Sayısı</p>
                                    </div>
                                </div>
                            </div>
                        </div>
                    }
                    else
                    {
                        <div class="text-center p-4">
                            <div class="mb-3">
                                <i class="fas fa-clipboard-list fa-3x text-muted"></i>
                            </div>
                            <p class="text-muted">Bu stok için henüz üretim kaydı bulunmamaktadır.</p>
                        </div>
                    }
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        $(document).ready(function () {
            $('#dailyEntriesTable').DataTable({
                language: {
                    url: '//cdn.datatables.net/plug-ins/1.10.24/i18n/Turkish.json'
                },
                "order": [[1, "desc"]],
                "pageLength": 10
            });
            $('[data-toggle="tooltip"]').tooltip();
            // Auto-hide alerts after 5 seconds
            setTimeout(function () {
                $(".alert").alert('close');
            }, 5000);
        });
    </script>
}